<nav id="site-nav">
    <div id="left-navs" class="navs-container">
        <div class="nav-button">
            <a href="/home">Home</a>
        </div>
        <div class="nav-button">
            <a id="systems-dropdown-button" class="dropdown-toggle" href="#" role="button">Systems▾</a>
            <ul id="systems-dropdown" class="dropdown-list hidden">
                {{#each systems}}
                    {{> dropdownEntry}}
                {{/each}}
            </ul>
        </div>
        {{#if loggedin}}
        {{else}}
            <div class="nav-button">
                <a id="login-toggle">Login</a>
            </div>
        {{/if}}
    </div>
    <div id="right-navs" class="navs-container">
        {{#if loggedin}}
            <div id="user-greeting">Hello, {{username}}</div>
            <div class="nav-button">
                <a id="logout-button">Logout</a>
            </div>
        {{/if}}
    </div>
</nav>